#include "mycustomplot.h"

/*
void MyCustomPlot::readSignalValue(double x) {
    if (graph(0)) { // Pretpostavimo da imamo jedan graf
        auto data = graph(0)->data();
        auto it = data->findBegin(x); // Pronađi točku najbližu x poziciji
        if (it != data->constEnd()) {
            double y = it->value; // Očitaj y vrijednost
            qDebug() << "Signal value at x =" << x << ", y =" << y;
        }
    }
}
*/
